import React from "react";
import {Container} from './styled'

const ListAtendidos: React.FC = ({children}) => {
    return(
        <Container>
            {children}
        </Container>
    )

}

export default ListAtendidos
